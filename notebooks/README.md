# Notebooks

- `PCA_GMM_Fintech_Tesis_v2.ipynb`: construcción del índice (PCA), panel two-way FE, diagnósticos (Nickell, Pesaran CD, Driscoll-Kraay), submuestra prepandemia. Pendiente de subir por el autor.

Convención: los notebooks se guardan con las salidas limpias (`jupyter nbconvert --clear-output`) para que el diff sea legible; las figuras y tablas finales se exportan a `paper/` o `data/processed/`.
