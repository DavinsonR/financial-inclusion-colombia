# Datos procesados

Panel limpio, índice IIF_Multidim por departamento y periodo, y cualquier tabla intermedia que el notebook necesite. Se versionan cuando su tamaño y licencia lo permitan; cada archivo debe poder regenerarse desde `data/raw/` con el código de `src/` o `notebooks/`.
